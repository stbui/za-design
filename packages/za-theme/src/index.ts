import defaultTheme from './default';
import lightTheme from './light';
export * from './useTheme';

export const themes = {
    default: defaultTheme,
    light: lightTheme,
};

export { lightTheme, defaultTheme };
